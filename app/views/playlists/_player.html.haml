.player-container{ ng_controller: "PlaybackCtrl" }
  .currently-playing.wordwrap{ ng_show: "player.currentSong && isPlayingPlaylist()" }
    {{player.currentSong.song_name}}
  .playback-controls
    %button.btn.btn-inverse.control.play{ ng_click: "playerAction('play')", ng_show: "player.state != 'playing' || !isPlayingPlaylist()" }
      = image_tag "audio_play.png"
    %button.btn.btn-inverse.control.pause{ ng_click: "playerPause()", ng_show: "player.state == 'playing' && isPlayingPlaylist()" }
      = image_tag "audio_pause.png"
    %button.btn.btn-inverse.control.skip{ ng_click: "playerAction('skip')" } Skip
  .progressbar-container{ ng_show: "player.currentSong && isPlayingPlaylist()" }
    .progressbar{ seek_progress_bar: "" }
      %span.bufferbar
      %span.timebar
    .time
      %span.current-time
      %span.duration
